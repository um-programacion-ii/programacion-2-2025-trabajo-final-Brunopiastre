spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/ticketflow
    username: ticket_user
    password: CHANGE_ME

  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8080

catedra:
  base-url: http://SERVIDOR_REAL:PUERTO
  jwt-token: PROD_TOKEN
